{
  "name": "dungeon-master",
  "private": true,
  "version": "0.0.0",
  "engines": {
    "node": ">=24",
    "pnpm": ">=10"
  },
  "packageManager": "pnpm@10.33.0",
  "scripts": {
    "lint": "turbo run lint",
    "test": "turbo run test",
    "build": "turbo run build",
    "format": "turbo run format"
  },
  "devDependencies": {
    "turbo": "^2"
  }
}
